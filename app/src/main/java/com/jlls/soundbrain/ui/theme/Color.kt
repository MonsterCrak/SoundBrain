package com.jlls.soundbrain.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================================
// SOUNDBRAIN MUSIC AI - ZEN MINIMALISM PALETTE
// ============================================================================
// Palette inspired by zen minimalism and music apps:
// - Soft violet primary for brand
// - Light airy backgrounds with high negative space
// - Glassmorphism support colors
// ============================================================================

// Primary Brand Colors - Soft Violet
val Primary = Color(0xFF8B7CF7)           // Soft violet
val PrimaryVariant = Color(0xFF6B5CE7)    // Darker variant
val OnPrimary = Color.White              // Text on primary

// Zen Background Colors - Light & Airy
val Background = Color(0xFFFAFBFC)          // Very light gray-white
val Surface = Color.White                   // Pure white cards
val SurfaceVariant = Color(0xFFF5F6F8)       // Subtle gray variant

// Text Colors
val OnBackground = Color(0xFF1A1B1E)        // Near black
val OnSurface = Color(0xFF1A1B1E)          // Near black
val OnSurfaceVariant = Color(0xFF8E9095)   // Muted gray

// Accent Colors for Music AI
val AccentCyan = Color(0xFF00D9FF)          // Electric cyan for highlights
val AccentPink = Color(0xFFFF6B9D)          // Soft pink for accents

// Glassmorphism Colors
val GlassBackground = Color(0xCCFFFFFF)     // 80% white alpha
val GlassBorder = Color(0x1A000000)          // 10% black border

// Divider & Border - Subtle 1dp borders
val Divider = Color(0xFFE8E9EB)
val Border = Color(0xFFEAEBED)

// Card Colors
val CardBackground = Color.White
val CardShadow = Color(0x1A000000)

// ============================================================================
// DARK THEME COLORS
// ============================================================================

val DarkPrimary = Color(0xFFA594FF)      // Lighter violet for dark mode
val DarkOnPrimary = Color(0xFF1A1B1E)    // Dark text on light primary
val DarkBackground = Color(0xFF0F0F12)   // Very dark
val DarkSurface = Color(0xFF1A1A1F)     // Dark surface
val DarkSurfaceVariant = Color(0xFF252529) // Darker variant
val DarkOnBackground = Color(0xFFF5F5F7) // Light text on dark
val DarkOnSurface = Color(0xFFF5F5F7)    // Light text on dark surface
val DarkOnSurfaceVariant = Color(0xFF8E9095) // Muted light text
val DarkDivider = Color(0xFF2A2A30)      // Dark divider
val DarkBorder = Color(0xFF3A3A42)       // Dark border
val DarkGlassBackground = Color(0xCC1A1A1F) // For glassmorphism in dark mode
